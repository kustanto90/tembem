{
  "name": "github-trending-daily",
  "version": "0.0.1",
  "description": "Get Github Trending items",
  "main": "index.js",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "build-gh-pages": "git checkout master -- data/ && rm -r ./public/ && cp -r ./data/ ./public/ && rm -r ./data/ && next build && next export -o ./docs/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brian Bagdasarian",
  "license": "MIT",
  "dependencies": {
    "next": "^10.0.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
